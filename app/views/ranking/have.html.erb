<div class="container">
  <div class="container-inner inner-default clearfix">
    <% @ranking_have.each.with_index(1) do |have, rank| %>
      <div class="item-box">
        <div class="rank">
          第<%= rank %>位
        </div>
        <div class="image">
          <div class="img-wrap">
            <% item = Item.find(have[0]) %>
            <% if item.large_image.present? %>
              <%= image_tag(item.large_image , alt: item.title) %>
            <% else %>
              <%= image_tag("no-image.png" , alt: item.title) %>
            <% end %>
          </div>
        </div>
        <div class="detail">
          <p class="title">
            <%= item.title %>
          </p>
        </div>
        <%= render 'items/action' , item: item if logged_in? %>
      </div>
    <% end %>
  </div>
</div>